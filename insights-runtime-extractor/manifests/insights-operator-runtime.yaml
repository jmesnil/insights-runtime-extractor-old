apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: insights-runtime-extractor
  labels:
    app.kubernetes.io/name: insights-runtime-extractor
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: insights-runtime-extractor
  template:
    metadata:
      labels:
        app.kubernetes.io/name: insights-runtime-extractor
    spec:
      serviceAccountName: insights-runtime-extractor-sa
      hostPID: true
      # You must have create a secret named insights-runtime-extractor-pull-secret to be able to pull the image
      imagePullSecrets:
        - name: insights-runtime-extractor-pull-secret 
      containers:
        - name: insights-runtime-extractor
          # Replace the image field it is is coming from another image registry
          image: ghcr.io/jmesnil/insights-runtime-extractor:latest
          imagePullPolicy: Always
          env:
            - name: CONTAINER_RUNTIME_ENDPOINT
              value: unix:///crio.sock
          securityContext:
            privileged: true
            capabilities:
              drop:
                - ALL
              add:
                - CAP_SYS_ADMIN
          volumeMounts:
            - mountPath: /crio.sock
              name: crio-socket
      volumes:
        - name: crio-socket
          hostPath:
            path: /run/crio/crio.sock
            type: Socket